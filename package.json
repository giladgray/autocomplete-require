{
  "name": "autocomplete-require",
  "version": "0.0.0",
  "description": "Atom autocomplete provider for require()'d modules",
  "main": "./lib/main",
  "license": "MIT",
  "repository": "https://github.com/giladgray/autocomplete-require",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  }
}
